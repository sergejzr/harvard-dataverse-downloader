#!/bin/sh
set -e

APP_DIR="/opt/dataverse-downloader"
SYSTEM_DESKTOP="/usr/share/applications/dataverse-downloader.desktop"

if [ -f "$APP_DIR/lib/dataverse-downloader-dataverse-downloader.desktop" ]; then
    install -Dm644 \
        "$APP_DIR/lib/dataverse-downloader-dataverse-downloader.desktop" \
        "$SYSTEM_DESKTOP"
fi

if command -v update-desktop-database >/dev/null 2>&1; then
    update-desktop-database /usr/share/applications >/dev/null 2>&1 || true
fi

if command -v xdg-desktop-menu >/dev/null 2>&1; then
    xdg-desktop-menu forceupdate >/dev/null 2>&1 || true
fi

exit 0